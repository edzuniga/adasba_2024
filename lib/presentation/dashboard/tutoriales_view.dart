import 'package:flutter/material.dart';

class TutorialesView extends StatelessWidget {
  const TutorialesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tutoriales VIEW'),
    );
  }
}
